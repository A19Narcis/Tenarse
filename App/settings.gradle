pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://repo.mongodb.org/maven2/"
        }
        maven {
            url 'https://jitpack.io'
        }
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Tenarse"
include ':app'
